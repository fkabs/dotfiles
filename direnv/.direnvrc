layout_venv() {
    local venv_path="$1"
    
    if [ -s "$VIRTUALENV_DIR" ]; then
        venv_path="${VIRTUALENV_DIR}/$1"
    fi
    
    source ${venv_path}/bin/activate
    
    # https://github.com/direnv/direnv/wiki/PS1
    unset PS1
}
