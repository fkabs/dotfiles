#!/bin/zsh
#
# .zconfig - Zsh config file for custom configs.
#

# Asdf (load)
. $(brew --prefix asdf)/libexec/asdf.sh

# Direnv (load)
source "${XDG_CONFIG_HOME:-$HOME/.config}/asdf-direnv/zshrc"

# History config
export HISTFILE=${XDG_CACHE_HOME:-$HOME/.cache}/zsh/.zsh_history
export HISTSIZE=1000000
export SAVEHIST=1000000 

# Virtualenvwrapper config
export WORKON_HOME=$HOME/.virtualenvs
export PROJECT_HOME=$HOME/work
export VIRTUALENVWRAPPER_SCRIPT=$(asdf where python)/bin/virtualenvwrapper.sh
export VIRTUALENVWRAPPER_PYTHON=$(asdf which python)
. $(asdf where python)/bin/virtualenvwrapper_lazy.sh

# Set default virtualenv
workon base

# Powerlevel10k config
# To customize prompt, run `p10k configure` or edit ~/.dotfiles/zsh/p10k/.p10k
[[ -e ${ZDOTDIR:-$HOME}/p10k/.p10k ]] && source ${ZDOTDIR:-$HOME}/p10k/.p10k
